
.titlebar {
  position: relative;
  width: 100%;
  height: 70px;
}

.titlebar .querystatus {
  position: absolute;
  margin: 10px 5px 10px 20px;
  padding: 10px;
  border: 1px dashed #fece3c;
  border-radius: 10px;
}


.titlebar .close:after {
  font-size: 0.65rem;
  content: ' ✕ '; /* ' \2715'; */
  vertical-align: super;
  cursor: pointer;
}

.titlebar .legend {
  position: absolute;
  margin-left: 430px;
}

.titlebar .legend text {
  font-family: sans-serif;
  font-size: 9px;
}

.titlebar .legend .qtile .cumulative {
  visibility: hidden;
}

.titlebar:hover .legend .cumulative {
  visibility: visible;
}